## How to run
* Install Dependencies
        npm install -g expo-cli
        npm install -g yarn
        yarn
* Run project
        yarn start

## How to build to google play store

* Install Dependencies
        npm install -g eas-cli
        npm install -g yarn
        yarn
* Run 
        eas build -p android
